import { SecretLintCoreResultMessage } from "@secretlint/types";
/**
 * sort messages by line and column
 */
export declare function sortMessagesByLocation(messages: SecretLintCoreResultMessage[]): SecretLintCoreResultMessage[];
